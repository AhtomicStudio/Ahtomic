Input — labeled text field on the darker `--black-900` well; red focus ring.

```jsx
<Input label="Email" placeholder="you@company.com" hint="We reply within two days." />
<Input label="About the project" textarea placeholder="What are you building?" />
```

`error="…"` swaps the hint and reddens the border. Native input props pass through.
