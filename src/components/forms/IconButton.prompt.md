IconButton — square icon-only button for toolbars, nav, dismissals. Always pass `label`.

```jsx
<IconButton label="Close" variant="ghost"><XIcon /></IconButton>
```

Same variants/sizes as Button. Icon should be a Lucide glyph at 16–20px using currentColor.
