Card — the standard surface: `--surface-card`, 1px `--line-1`, 14px radius, no shadow.

```jsx
<Card padding="lg"><h3>Title</h3><p>Body.</p></Card>
<Card href="#" interactive>Hover brightens the border.</Card>
```
