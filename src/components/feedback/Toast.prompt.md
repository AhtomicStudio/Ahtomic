Toast — quiet notification with a colored tone bar.

```jsx
<Toast tone="positive" onDismiss={hide}>Message sent. We'll be in touch.</Toast>
```

Float it with `style={{position:"fixed", bottom:24, right:24}}`.
